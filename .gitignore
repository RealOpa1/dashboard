// Сохранение данных при отсутствии сети
async function saveActivity(data) {
  if (!navigator.onLine) {
    await indexedDB.add('activities', data); 
    showToast('Данные сохранены для отправки позже');
  } else {
    sendToServer(data); 
  }
}
